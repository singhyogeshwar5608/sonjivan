========================================
FIREBASE SECURITY RULES - UPDATED WITH CATEGORIES
========================================

⚠️ COPY THESE RULES TO FIREBASE CONSOLE

Project: sonjivan-27dff
Go to: https://console.firebase.google.com

========================================
FIRESTORE DATABASE RULES
========================================

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Helper function to check if user is admin
    // Checks the 'role' field in users collection
    function isAdmin() {
      return request.auth != null && 
             exists(/databases/$(database)/documents/users/$(request.auth.uid)) &&
             get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
    }
    
    // Products Collection - Public read, Admin write
    match /products/{productId} {
      allow read: if true;  // Anyone can view products
      allow create, update, delete: if isAdmin();  // Only admin can manage
    }
    
    // Categories Collection - Public read, Admin write
    match /categories/{categoryId} {
      allow read: if true;  // Anyone can view categories
      allow create, update, delete: if isAdmin();  // Only admin can manage
    }
    
    // Services Collection - Public read, Admin write
    match /services/{serviceId} {
      allow read: if true;  // Anyone can view services
      allow create, update, delete: if isAdmin();  // Only admin can manage
    }
    
    // Gallery Collection - Public read, Admin write
    match /gallery/{imageId} {
      allow read: if true;  // Anyone can view gallery
      allow create, update, delete: if isAdmin();  // Only admin can manage
    }
    
    // Inquiries Collection - Public create, Admin read/update/delete
    match /inquiries/{inquiryId} {
      allow create: if true;  // Anyone can submit inquiry
      allow read, update, delete: if isAdmin();  // Only admin can manage
    }
    
    // Orders Collection - Public create, Admin read/update/delete
    match /orders/{orderId} {
      allow create: if true;  // Anyone can place order
      allow read, update, delete: if isAdmin();  // Only admin can manage
    }
    
    // Users Collection - Users can read their own data, Admin can access all
    match /users/{userId} {
      allow read: if request.auth != null && 
                     (request.auth.uid == userId || isAdmin());
      // Allow authenticated users to create/update their own document
      allow create, update: if request.auth != null && request.auth.uid == userId;
      // Admin can manage all users
      allow write: if isAdmin();
    }
    
    // Admin Collection - Only admin can access
    match /admin/{document=**} {
      allow read, write: if isAdmin();
    }
    
    // Testimonials Collection - Public read, Admin write
    match /testimonials/{testimonialId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }
    
    // Settings Collection - Public read, Admin write
    match /settings/{settingId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }
    
    // Upcoming Products Collection - Public read, Admin write
    match /upcomingProducts/{productId} {
      allow read: if true;  // Anyone can view upcoming products
      allow create, update, delete: if isAdmin();  // Only admin can manage
    }
    
    // Default deny all other collections
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

========================================
HOW TO APPLY IN FIREBASE CONSOLE
========================================

Step 1: Open Firebase Console
   → Go to: https://console.firebase.google.com
   → Select project: sonjivan-27dff

Step 2: Navigate to Firestore Rules
   → Click "Firestore Database" in left sidebar
   → Click "Rules" tab at the top

Step 3: Replace Rules
   → DELETE all existing rules in the editor
   → COPY the rules above (from "rules_version = '2';" to the last closing brace)
   → PASTE into the Firebase rules editor

Step 4: Publish Rules
   → Click "Publish" button
   → Wait for "Rules published successfully" message
   → Rules are now active!

========================================
VERIFY YOUR ADMIN USER
========================================

Your admin user MUST have these exact fields in Firestore:

Collection: users
Document ID: jAAnrFLr0GSxIayTfNMnwcDMxAw2 (your UID)

Fields:
{
  "email": "yogi@gmail.com",
  "name": "yogi",
  "role": "admin",        ← MUST be exactly "admin" (lowercase)
  "isActive": true,
  "createdAt": (timestamp),
  "updatedAt": (timestamp)
}

⚠️ CRITICAL: The "role" field MUST be exactly "admin" (lowercase, no spaces)

To verify:
1. Firebase Console → Firestore Database
2. Click "users" collection
3. Find document: jAAnrFLr0GSxIayTfNMnwcDMxAw2
4. Check that role = "admin"
5. If not, click the document and edit the "role" field to "admin"

========================================
WHAT'S NEW IN THESE RULES
========================================

✅ Added Categories Collection
   - Public read access (anyone can view categories)
   - Admin-only write access (create, update, delete)
   - Same security pattern as products and services

✅ All Collections Included:
   - products (grocery items)
   - categories (product categories)
   - services (aluminum services)
   - gallery (images)
   - inquiries (contact forms)
   - orders (customer orders)
   - users (user accounts)
   - testimonials (customer reviews)
   - settings (site settings)
   - upcomingProducts (coming soon products) ← NEW

✅ Dynamic Admin Check:
   - No hardcoded emails
   - Checks Firestore users collection for role
   - Works with any user that has role: "admin"

========================================
TESTING AFTER APPLYING RULES
========================================

Step 1: Logout from Admin Panel
   → Go to admin panel
   → Click logout

Step 2: Clear Browser Cache
   → Press Ctrl + Shift + Delete
   → Clear "Cached images and files"
   → Clear "Cookies and other site data"
   → Click "Clear data"

Step 3: Login Again
   → Go to: http://localhost:5174/admin/login
   → Email: yogi@gmail.com
   → Password: (your password)
   → Click "Sign In"

Step 4: Test All Operations
   ✅ Add Product - should work
   ✅ Add Category - should work
   ✅ Add Service - should work
   ✅ Upload Gallery Image - should work
   ✅ View Inquiries - should work

========================================
TROUBLESHOOTING
========================================

Error: "Missing or insufficient permissions"
   → Check that you're logged in as admin
   → Verify Firestore user document has role: "admin"
   → Clear browser cache and login again
   → Check browser console (F12) for detailed error

Error: "User data not found"
   → Firestore users document doesn't exist
   → Create document with UID as document ID
   → Add role: "admin" field

Error: "Access denied"
   → User exists but role is not "admin"
   → Edit Firestore document and set role: "admin"

Still not working?
   → Open browser console (F12)
   → Try the operation that's failing
   → Copy the error message
   → Check Firebase Console → Firestore → Rules → Playground
   → Test the rules with your user UID

========================================
FIREBASE STORAGE RULES (OPTIONAL)
========================================

If you're using Firebase Storage for images, also update Storage rules:

Go to: Firebase Console → Storage → Rules

rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    
    // Helper function to check if user is admin
    function isAdmin() {
      return request.auth != null && 
             firestore.get(/databases/(default)/documents/users/$(request.auth.uid)).data.role == 'admin';
    }
    
    // Allow public read, admin write
    match /{allPaths=**} {
      allow read: if true;
      allow write: if isAdmin();
    }
  }
}

========================================
SUMMARY
========================================

✅ Services page now fetches products from Firebase products collection
✅ Categories page created with website color scheme
✅ Categories link added to header navigation
✅ Category Management page created in admin panel
✅ Categories link added to admin sidebar
✅ Admin can perform CRUD operations on categories
✅ Coming Soon Products feature implemented
   - New upcomingProducts collection created
   - Public read access for viewing upcoming products
   - Admin-only write access for managing upcoming products
   - Products can be moved from upcoming to active status
   - Coming Soon section always shows on Home page with placeholder when empty

Collections with Public Read + Admin Write:
- products
- categories
- services
- gallery
- testimonials
- settings
- upcomingProducts ← NEW

Collections with Public Create + Admin Manage:
- inquiries
- orders

Your admin user: yogi@gmail.com
UID: jAAnrFLr0GSxIayTfNMnwcDMxAw2
Role: admin (must be set in Firestore)

After applying these rules, all operations will work! 🎉

========================================
